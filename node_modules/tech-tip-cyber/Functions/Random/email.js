const toString = require('to-str');
const randomLorem = require('random-lorem');
const randomDoamins = require('random-domains');

function randomEmail(options) {
  const domain = options && options.domain
    ? toString(options.domain)
    : randomDoamins();

  return randomLorem() + '@' + domain;
};

module.exports = randomEmail;